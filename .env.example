# Required: Telegram Bot Token (from @BotFather)
TG_BOT_TOKEN=your_bot_token_here

# Required: Your Telegram user ID (get it from @userinfobot)
ALLOWED_USER_ID=123456789

# Optional: Working directory for Claude Code execution
WORKDIR=/home/pi/projects

# Optional: Path to file containing unlock password
SECRET_FILE=/home/pi/claude-bot/.secret

# Optional: Unlock duration in minutes (default: 10)
UNLOCK_TTL_MIN=10

# Optional: Execution quantum in minutes before pause (default: 10)
QUANTUM_MIN=10

# Optional: State file path for persistence
STATE_FILE=/home/pi/claude-bot/state/state.json

# Optional: Log directory for task logs
LOG_DIR=/home/pi/claude-bot/logs

# Optional: Status update interval in seconds (default: 25)
HEARTBEAT_SEC=25

# Optional: Number of output lines to show in status (default: 20)
TAIL_LINES=20
